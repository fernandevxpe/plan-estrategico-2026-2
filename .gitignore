.env.local
secrets/
*.pem
*.key
*.crt
*.p12
*.pfx
data/raw/
data/processed/
node_modules/
.next/
.DS_Store
.vercel
.env*
tsconfig.tsbuildinfo
*.tsbuildinfo
next-env.d.ts

# Arquivo de segredos: nunca versionar, nunca abrir.
secrets.zip

# Estado de execução do agendador (scripts/scheduler.mjs grava em DATA_DIR).
# É runtime por máquina: local aponta para ./data, produção para o volume do
# Railway. Versionar congelaria o resultado de uma execução desta máquina — o
# arquivo aqui hoje registra um sync que FALHOU em 25/08.
data/sync-state.json

# Fotos locais do cadastro manual (scripts/snapshot-cadastro-manual.mjs).
# Dado financeiro nominal do time — fica na máquina, nunca no GitHub.
backups/
